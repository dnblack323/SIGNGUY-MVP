# SignGuy AI — PRD

**Product:** SignGuy AI — the permanent commercial business-management platform for sign, graphics, wrap, print, and apparel shops.
**Repository:** `dnblack323/SIGNGUY-MVP` (permanent product). Donor repos are read-only reference material.
**Stack:** FARM (FastAPI + React + MongoDB).

## Original problem statement

Owner is building SignGuy AI as a permanent commercial application (not an MVP scaffold). Execution proceeds through numbered checkpoints defined, for EC0–EC8, in `SIGNGUY_AI_FINAL_CONSOLIDATED_MASTER_BUILD_PLAN.md` (all COMPLETE/CLOSED), and, for **EC9 onward, in the owner-approved SignGuy AI Checkpoint Specification Pack** (`/app/specs_pack/extracted/*.docx` — Master Index + EC09–EC22), which supersedes the old master plan's condensed EC9–EC14 sections. See `/app/memory/checkpoint_reference_table.md` for the full renumbered sequence, `/app/memory/documentation_authority_register.md` for the priority order of every planning document, and `/app/memory/owner_specification_hold_register.md` for active owner holds and open contradictions. Each checkpoint requires a preflight, code, tests, evidence package, and explicit stop before the next checkpoint — **no checkpoint begins without explicit owner authorization.**

Constraints:
- No `Job / Job Item / Job Ticket / Production Ticket / Job Ticket Summary` terminology.
- Commerce values stored as integer cents with `_cents` suffix; pricing configuration remains float dollars.
- Fail-closed production guards enforced by `security_guards.py`.
- Strict tenant isolation on every read/write.
- Backend enforcement is authoritative for permissions.
- No wholesale donor copies.

## Checkpoint status (2026-02)

| Checkpoint | Status | Evidence |
|---|---|---|
| EC0 — Owner Decisions | COMPLETE | Baked into master plan §4 |
| EC1 — Security & Guardrails | COMPLETE | `/app/evidence/EC1_evidence.md` |
| EC2 — Shared Platform Services | COMPLETE | `/app/evidence/EC2_evidence.md` |
| EC3 — Quotes, Orders, Order Items, Pricing Snapshots | COMPLETE | `/app/evidence/EC3_evidence.md` |
| EC4 — Invoices, Payments, and Stripe Core | COMPLETE | `/app/evidence/EC4_evidence.md` |
| EC5 — Production and Work Orders | COMPLETE | `/app/evidence/EC5_evidence.md` |
| **EC6 — Asset Library, Proofs, Signatures, Customer Portal** | **COMPLETE** | `/app/evidence/EC6_evidence.md` — 161/161 backend + `testing_agent_v3_fork` iteration 9 100% pass |
| **EC3.1 — Pricing Foundation Verification & Full Calculator Category Coverage** | **REQUIRED — SCHEDULED (permanent scope)** | Master plan Appendix A.2 — every calculator category, formulas, shop rate/labor/materials/waste/markup/margin/minimums/complexity/add-ons/templates, snapshots, tests. Must land before EC14. |
| **EC6.3 — Order Intake Capture & Visual Markup** | **REQUIRED — SCHEDULED (permanent scope)** | Master plan Appendix A.1 — image + camera + PDF uploads, drawing on images + blank canvas, version history, attach to Proofs/WOs/WOSummaries, controlled portal visibility, in-person signature capture bound to exact target with immutable audit. Reuses EC2 + EC6, no parallel system. Must land before EC14. |
| EC6.2 — Signed PDF Composite Rendering | DEFERRED (unscheduled) | `/app/memory/product_ideas_register.md` — reconsider during EC14 Final Hardening, or earlier only on a verified customer/compliance/operational requirement. Do NOT schedule during EC7. |
| **EC7 — Inventory, Purchasing, Finance, Reporting** | **COMPLETE** (all phases 7a+7b+7c+7d delivered; frontend closure workflows landed; `testing_agent_v3_fork` regression PASS) | `/app/evidence/EC7_evidence.md` + `/app/docs/modules/EC7_INVENTORY_PURCHASING_FINANCE.md`. Backend 215/215 green. Frontend Jest 25/25 green. Regression report `/app/test_reports/iteration_10.json`. |
| **EC8 — Team, Scheduling, Time, Payroll, Employee Portal, Equipment Training & Certification** | **DELIVERED / CLOSED — all phases 8a–8f complete.** | `/app/evidence/EC8_evidence.md`. Employee CRUD + status transitions, Announcements, Team Dashboard (8a) + Time Clock + Timesheets (8b) + Scheduling & Employee Portal (8c) + Payroll (8d) + Equipment/Training/Certification + Work Order enforcement (8e) + final closure regression (8f) — 312/312 full backend pytest green + terminology guard OK. Phase 8f ran the first full frontend regression covering Phase 8e (`testing_agent_v4` iteration_15): found 3 Phase-8e-only defects (critical quiz-submission 500; medium Certifications Matrix stale-dialog; low a11y DialogTitle warning), all fixed and confirmed via a focused retest (iteration_16, 100% pass, 0 open defects). Zero regressions in EC0-EC7 or Phase 8a-8d. EC8 formally closed. |
| EC9–EC14 (old numbering) | **SUPERSEDED — HISTORICAL REFERENCE ONLY — NOT IMPLEMENTATION AUTHORITY** | Replaced 2026-02 by the new EC9–EC22 sequence from the SignGuy AI Checkpoint Specification Pack. See `/app/memory/checkpoint_reference_table.md`. |
| EC9–EC22 (new numbering) | **EC9 — ALL PHASES (9A–9H) COMPLETE / CLOSED — owner-accepted 2026-02.** **EC10 — ALL PHASES (10A–10H) COMPLETE / CLOSED (2026-07-16).** **EC11 — IN PROGRESS; Phase 11A COMPLETE (2026-07-16); Phase 11B COMPLETE (2026-07-16); Phase 11C COMPLETE (2026-07-16); Phase 11D NOT STARTED.** Phase 10E was split into controlled subphases 10E-1..10E-5 and closed before 10F. EC12 NOT STARTED. | Controlling docs at `/app/specs_pack/extracted/*.docx`. Full status/holds table: `/app/memory/checkpoint_reference_table.md`. Active owner holds: `/app/memory/owner_specification_hold_register.md`. EC10 preflight: `/app/preflight/EC10_ORDER_INTAKE_VISUAL_MARKUP_CUSTOMER_DECISION_ROOM_AND_TEMPLATES_PREFLIGHT.md`. Phase completion reports: `/app/evidence/EC10_PHASE10A_COMPLETION_REPORT.md`, `/app/evidence/EC10_PHASE10B_COMPLETION_REPORT.md`, `/app/evidence/EC10_PHASE10C_COMPLETION_REPORT.md`, `/app/evidence/EC10_PHASE10D_COMPLETION_REPORT.md`, `/app/evidence/EC10_PHASE10E1_COMPLETION_REPORT.md`, `/app/evidence/EC10_PHASE10E2_COMPLETION_REPORT.md`, `/app/evidence/EC10_PHASE10E3_COMPLETION_REPORT.md`, `/app/evidence/EC10_PHASE10E4_COMPLETION_REPORT.md`, `/app/evidence/EC10_PHASE10E_CLOSURE_REPORT.md`, `/app/evidence/EC10_PHASE10F_COMPLETION_REPORT.md`, `/app/evidence/EC10_PHASE10G_COMPLETION_REPORT.md`, `/app/evidence/EC10_CLOSURE_REPORT.md`, `/app/evidence/EC11_PHASE11A_COMPLETION_REPORT.md`, `/app/evidence/EC11_PHASE11B_COMPLETION_REPORT.md`, `/app/evidence/EC11_PHASE11C_COMPLETION_REPORT.md`. |

## Completed capabilities

- Auth / Tenants / Users / Permissions (staff + platform + portal scopes) — EC1.
- Startup guards, terminology guard, money helpers, LOCKED navigation shell — EC1.
- Settings / Activity / Notifications / SendGrid webhook / Upload validation / File+Document links / Feature entitlements / Integration status — EC2.
- Quote line items + revisions + expiration + approval-state foundation — EC3.
- Rich Order Item schema with backend-derived totals, pricing snapshots, manual override with reason — EC3.
- `production_required` rule + override + reason — EC3.
- Idempotent race-safe Quote-to-Order conversion copying line items + snapshots + source revision — EC3.
- Work Order snapshot filters by `production_required` — EC3.
- **Functional frontend Quote/Order editors:** quick + detailed entry modes, calculator integration, override-reason validation, production-required switch, revision-warning dialog, expired-quote convert-with-override UX, source-quote link on Orders — EC3 (v2 corrections).
- Invoice dual-status (`document_status` + `financial_status`), reconciliation, `_cents` money, immutable snapshots — EC4.
- Payments: record / void / refund with idempotency; Stripe Core integration (test adapter, publishable-key + client_secret never leak to DOM/console) — EC4.
- **Work Order lifecycle (EC5):** 9-state enum, priority + due dates, immutable snapshots, versioned regenerate/supersede with reason, controlled transitions coordinating Order operational status, cross-tenant-safe assignment with in-app notifications, printable summary gated by `invoice:read`, `/api/production/board` view — EC5.
- **Production frontend (EC5):** Production Board (Kanban + HTML5 drag-drop + reason-required modal), rebuilt Work Order Detail (version banners, priority/due, allowed-transitions sidebar, assign dialog, in-page Print Summary), Generate WO dialog on Order Detail, Regenerate on both Order + WO detail, 9-state WO list filters + current-version toggle, Production Board sidebar link, all controls permission-gated via `/auth/me`.
- **Asset Library + Portal (EC6):** Documents metadata + versioning layered over existing FileRecord; scoped public-action tokens (single-purpose, expiring, revocable, hashed at rest); magic-link portal login (hashed, single-use, audience-scoped); portal JWT separation (`sub_scope="portal"`, disjoint dependency graph); Portal Identity n:1 mapping to Customer with 5 backend-authoritative permission-bundle presets; staff surfaces for Proofs (create/version/transition), Approvals (dual-parent incl. WOS), Signature Requests + Signatures; Public single-action endpoints (proof approve, sign, quote_view, invoice_view, public quote request, customer intake with staged-changes review); Portal customer routes (Quotes/Orders/Invoices/Documents/Proofs/Messages/Profile) with tenant+customer scope on every query; Portal messages via existing email service (no new messaging schema); Portal shell + login/magic-link/verify + list pages at `/portal/*` and public token pages at `/p/*`, both mounted OUTSIDE the staff `<AppShell>`.

- **Scheduling + Employee Portal (EC8 Phase 8c):** `Schedule`/`Shift` models (one Schedule per tenant per Sat–Fri week), manager Team Schedule builder (add/edit/cancel/copy shift/copy day/copy week/assign multiple/publish/republish), hard conflict blocks (duplicate/overlap/invalid/inactive/cross-tenant) + soft availability-warning with authorized override (audited). Employee Portal is additive on EC6's `PortalIdentity` (new `portal_type` discriminator — no second identity/token system) covering Dashboard, Time Clock (reused from 8b, zero duplicated logic), My Schedule (published-only, self-scoped), My Timesheet (self, payroll-rate-derived fields stripped), Announcements (audience/target-filtered), Profile — with a documented "My Tasks" boundary placeholder (no Task system exists). Separate `/portal/employee/*` shell + `sg_employee_portal_token` localStorage key from the Customer Portal.

- **Payroll (EC8 Phase 8d, 2026-07):** Internal gross-pay ledger only — NOT payroll-processing/tax-filing/banking (no ACH, no bank/SSN storage, no tax withholding, no W-2/1099). `PayPeriod` (one per tenant per Sat–Fri week, Friday payday, structurally overlap-free via unique `(tenant_id, start_date)` index) with statuses `open→review→approved→partially_paid→paid→closed` (+`voided`) — `partially_paid`/`paid` are ALWAYS derived from the append-only `PayrollTransaction` ledger (`_refresh_period_financial_status`), never directly settable. `PayrollSnapshot` is a pure read-model recomputed from the ledger (`_sum_ledger`/`_money_totals`); recalculation while open/review is idempotent (never duplicates `earning`/`overtime_earning` rows — only regenerates when the computed amount changes) and freezes on approval (reopen requires `payroll:manage` + a mandatory audited reason). Blocking-warning gate (`missing_rate`/`timesheet_not_approved`/`open_time_entries`/`overlapping_entries`/`not_recalculated`) on approve/close, overridable with a required reason (audited). Overtime: 40hr/week + 1.5x tenant default via the existing Settings `payroll` namespace, with per-Employee override. Carryover on close: immediate `carryover_in` if the next Pay Period exists, else a `payroll_carryovers` pending record that auto-links (idempotent, no duplicate creation) when that period is later created. Manual ledger entry types exposed to managers: `adjustment`/`advance`/`advance_repayment`/`payment` (idempotency-key support); voids/corrections always append an offsetting `void` row, never mutate history. Reuses EC7 Reports registry + generic CSV export for 2 new report keys (`payroll.by_period`, `payroll.by_employee`) — no parallel export system. Employee Portal "My Pay" added to the existing `portal_employee.py` router (no new router file) with a strict field allow-list (never other employees/manager notes/audit internals/bank/tax data). Manager UI: `/team/payroll` (Pay Periods / Employee Ledger / Settings tabs), Payroll tab on Employee Detail, Payroll card on Team Dashboard. 13 targeted backend pytest tests (`tests/test_ec8d_payroll.py`) + 60-test regression, all green.

- **Equipment, Training & Certification + Work Order enforcement (EC8 Phase 8e, 2026-07):** `Equipment` (category/access_policy/safety_sensitive/status), `TrainingDefinition` (reading/video/sop_review/quiz/practical_demonstration/manager_signoff/retraining, optional quiz authoring + `passing_score`, optional `practical_signoff_required`), `TrainingAssignment` lifecycle (`assign→start→complete|quiz→(pending_signoff)→signoff|fail|cancel`, quiz attempt history preserved, retry-after-fail correctly re-opens the assignment), `Certification` (issue/renew/revoke with permanent history, auto-expiry, `expires_soon` window). Work Order `assign()` runs `certification_service.check_work_order_assignment` against every proposed assignee's Certification for each of the Work Order's `required_equipment_ids`: `no_required`/`recommended` never block; `required_no_override` + no valid Certification → hard `409 assignment_blocked` (not overridable); `required_override_allowed` + no valid Certification → `409 assignment_warning_override_required`, assignable only with a non-empty `override_reason`; `required_role` mismatch is advisory-only (never blocks). The frontend `AssignDialog` renders the backend's `check.results` verbatim (block=rose/non-overridable, warning=amber/override-gated) — the enforcement rule is never re-implemented client-side. Manager UI: Equipment list/detail (`/team/equipment`), Training Definitions+Assignments incl. quiz builder (`/team/training`), Certification Matrix + All Certifications with issue/renew/revoke (`/team/certifications`), Work Order "Assignment requirements" card + edit dialog. Employee Portal: My Training (list + quiz-taking assignment detail) and My Certifications (read-only, renewal-needed flag), both strictly self-scoped (never accepts an Employee ID from the client, never exposes quiz answer keys/manager notes/override audit detail to the portal). Two real bugs found and fixed this phase: a quiz-retry-after-fail 409 caused by `complete_assignment`'s terminal-status guard firing on a legitimate passing retry; and pre-existing Employee Portal identities missing the new Training/Certification permissions (re-invite now re-syncs permissions, no separate migration needed). 15 targeted backend pytest tests (`tests/test_ec8e_equipment_training.py`), plus live curl verification of both the hard-block and warning-override Work Order assignment paths.

- **EC8 Phase 8f — Final closure (2026-07):** First full backend (312/312 pytest) + full frontend (`testing_agent_v4`) regression across EC0–EC8. Found and fixed 3 Phase 8e-only defects: quiz-submission 500 (schema-less quiz question dicts lacked a stable `id`; fixed via server-side backfill on create/update + defensive lookup), Certifications Matrix stale-dialog-after-issue (decoupled dialog state), and a missing-DialogTitle a11y warning. Confirmed via retest: 100% pass, 0 open defects. EC8 formally closed.

- **Sign in with Google (2026-07):** Emergent-managed Google Auth added to the **main app staff login only** (`/login`), as an ADDITIONAL option alongside the existing email/password login (not a replacement). Flow: "Continue with Google" button redirects to `https://auth.emergentagent.com/?redirect=<origin>/`; on return, the URL fragment carries `#session_id=...`, detected at render time in `App.js` (`AppRoutes`) before any route/auth-guard decision, which renders `GoogleAuthCallback` instead of the normal `<Routes>`. The callback exchanges the one-time `session_id` for the Google profile via `POST /api/auth/google/session`, which bridges into the **existing JWT/tenant system** (same `TokenOut` shape as `/login`/`/register-tenant` — no second, cookie-based session mechanism was introduced). Lookup order: existing `google_id` → existing account by email (auto-links `google_id` to it, but ONLY if the email is unambiguous across tenants — see Foundation Hardening below) → first-time identity auto-creates a new tenant + `owner` User (random unusable `password_hash`, since Google accounts have no app password) per the owner's explicit choice. `User.google_id: Optional[str]` added (additive, backward compatible). `AuthContext.refresh()` skips its normal check while `#session_id=` is present, to avoid a login-page flash race. Verified: backend 401s on an invalid session_id (curl), full 312/312 backend regression green, and a live browser round-trip (invalid session_id → correct error handling → redirect to `/login`, confirmed via console network log). The real Google consent screen itself cannot be exercised by an automated sandbox tool — end-to-end human verification with a real Google account is recommended before relying on production sign-ups through this path.

- **EC9 Phase 9E-1 — Core Flat & Square-Foot Products (2026-07):** `pricing_flat_sqft.py` formula library for Banners, Rigid Signs, Digital Print, Cut Vinyl (dimensions/materials/finishing/complexity/quantity-tiers/rush/manual, dispatched from the single authoritative `calculate_pricing()`). `CategorySpecificFields.jsx` progressive-disclosure UI. 18 targeted pytest tests.

- **EC9 Phase 9E-2 — Apparel & Promotional Items, + 2 owner-required corrections (2026-07):**
  - **Apparel** (`pricing_apparel.py`): 8 garment/hat blanks x 2 brands each (exact EC09 5-tier quantity/placement sell tables), 9 decoration methods. HTV & Screen Print Transfer are `pricing_authority: "exact_table"` (the uploaded tier tables ARE the sell price); the other 7 are `"foundation_estimate"` (fully selectable, EC09 setup-fee/material-rate cost-plus, never presented as an exact table — **Correction 1**). Plus-size upcharge (auto-counted from a sizes breakdown), custom name/number, specialty finishes, rush, $60 minimum.
  - **Correction 1 detail:** DTF/Sublimation's per-sq-in decoration AREA (16/100/116/9 sq in) has no EC09 source — moved out of a buried code constant into each method's own `default_area_sqin_by_placement` (editable via the existing generic category-update endpoint), flagged `is_provisional_area_assumption`, surfaces a `calculation_warnings` entry, and is snapshotted in the result (`decoration_area_assumption_sqin`). Per-calculation override remains available.
  - **Promotional Items** (`pricing_promotional.py`): driven by the tenant's `PricingSavedItem` library (Phase 9D) — no second catalog. 4 pricing methods (tier/per-piece/flat-fee/manual); non-matching tier quantity → `requires_manual_price: true`, `selling_price: null` (never invented — verified against all 6 Standard Paper + 6 Magnetic Business Card starter tiers through the real `/api/pricing/calculate` endpoint).
  - **Correction 2 — idempotent additive default merge** (`services/pricing.py::_apply_additive_default_merge`): long-lived tenants automatically receive newly-introduced `category_defaults` keys on their next settings fetch — ADD missing keys only, never overwrite an existing (even stale) tenant-edited value, fast-paths to a no-op once `starter_default_version` matches, never touches historical Quote/Order pricing snapshots. "Reset to starter" remains available as an explicit owner action but is no longer the only path. Verified live against the pre-existing Dev Shop tenant (real production `pricing_settings` doc, created before this session, correctly received `garments`/`decoration_methods`/`vehicle_types`/`benchmark_prices` while its edited `minimum_charge` stayed untouched).
  - 22 (9E-2) + 28 (corrections + 9E-3, see below) targeted pytest tests, all green; frontend compiles clean.

- **EC9 Phase 9E-3 — Vehicle Graphics & Wrap Pricing (2026-07):** `pricing_vehicle_graphics.py` — pricing only (no Wrap Lab/EC15 workflows). Vehicle type (12 types incl. provisional `mini_van`/`box_truck_24`/`semi`/`other`) x coverage type (spot/partial/half/full/custom — custom borrows the nearest tier's operational defaults, never invents a benchmark $). Waste-adjusted material area, laminate, window-perf (measured sq ft only — never an invented window-area assumption), production/design/prep/removal/install/helper labor, overhead, "use higher of EC09 package/benchmark price or cost-plus result" (never inventing a benchmark for `mini_van`/`other`, which have none), rush (30% default), fleet quantity, manual override always available and never silently replaced by a benchmark. Every EC09-unspecified assumption (base sqft/install hours for the 4 provisional vehicle types) is flagged `is_provisional`, surfaced as a `calculation_warnings` entry, and editable via the same category-update path. 28 targeted pytest tests (shared with the two corrections above) + live curl E2E verification against the real deployed API, all green.

- **EC9 Phase 9F — Quote, Order, and Order Item Pricing Integration (2026-07):** Wired the completed Pricing Foundation/calculators into Quotes, Orders, Order Items, and the standalone Pricing Calculator — no duplicate pricing logic anywhere; every price is derived by the canonical `calculate_pricing()` engine. New shared `services/order_pricing.py` (`resolve_references` for EC7 Material/Pricing Component/Saved Item lookups + `build_item_pricing_fields` for the manual-vs-suggested/cost/profit/margin/snapshot logic) is called from Quote Line Item and Order Item create/update, and from the refactored `/pricing/calculate` endpoint (removing the duplicated resolution code that used to live in the router). `QuoteLineItem`/`OrderItem` gained additive fields: `item_name`, `category_inputs`, `material_profile_id`, `pricing_component_ids`, `saved_item_id`, `suggested_price_cents`, `manual_price_cents`, `selected_price_source` ("suggested"|"manual"), `pricing_status` ("manual"|"calculated"), `estimated_cost/profit_cents`, `estimated_margin_percent`, `calculation_warnings`, `source_labels`, `last_recalculated_at`, `price_selected_by_user_id`, `previous_pricing_snapshot`. Fully backward compatible — a plain `{description, quantity, unit_price_cents}` payload behaves exactly as before (all 27 pre-existing quote/order/work-order/invoice tests still pass unmodified). "Suggested" selection is backend-authoritative (the client cannot spoof `unit_price_cents` — the server recomputes and overwrites it); "manual" always wins when explicitly selected and is never silently overwritten. New `POST /quotes/{id}/line-items/{item_id}/recalculate-preview` and `POST /orders/{id}/items/{item_id}/recalculate-preview` (draft-only, pure compute, zero persistence) pair with the existing PATCH (now accepting `recalculate: true`) as the accept step — the prior snapshot is preserved in `previous_pricing_snapshot` before being replaced. `compute_pricing_summary()` (new, in `commerce_totals.py`) rolls up estimated cost/profit/margin/suggested-vs-manual totals/warning counts for the new `pricing_summary` block now returned by `GET /quotes/{id}` and `GET /orders/{id}`. `quote_conversion.py` now copies every new pricing field byte-for-byte — zero recalculation on Quote→Order conversion. Frontend: `LineItemDialog.jsx` (shared by Quote/Order) Detailed tab now embeds the same `CategorySpecificFields` + `SavedItemSelector` + `PricingComponentSelector` + a new inline canonical-material-profile picker used by the standalone calculator, with a suggested-vs-manual radio selector and a Recalculate panel (preview → Accept/Reject) for existing calculated items; Quick tab unchanged in spirit (item name/category/qty/price/optional saved item). `PricingCalculatorPage.jsx` gained "Add to Quote" / "Add to Order" buttons that post the calculated result to any existing draft document. `OrderDetailPage.jsx`/`QuoteDetailPage.jsx` totals blocks gained an additive pricing-summary section (est. cost/profit/margin/warnings-to-review) without disturbing the existing subtotal/discount/tax/total rows. 23 new targeted tests (`tests/test_ec9_phase9f_quote_order_integration.py`) + 27 directly-affected pre-existing tests (quotes/orders/work-orders/invoices) all green — zero regressions. Per explicit owner directive, `testing_agent`, full backend/frontend regression, and browser automation were NOT run for this phase.

- **EC9 Phase 9G — Immutable Pricing Snapshots + Advisory Contracts (2026-07):** Standalone Calculator gap closed first — extracted the Phase 9F `LineItemDialog` local `MaterialProfilePicker` into a shared `components/pricing/selectors/MaterialProfileSelector.jsx` (no duplicate selector) and added it + the existing `PricingComponentSelector` to `PricingCalculatorPage.jsx`; both IDs now flow into `/pricing/calculate` and, when "Add to Quote/Order" is used, into the created item's canonical pricing resolution. New **append-only** `pricing_snapshot_records` collection (`models/pricing_snapshot_record.py`, `services/pricing_snapshot_records.py`) gives every Quote Line Item / Order Item pricing write a full, standalone historical record (tenant/source/quote/order ids, category, quantity, category_inputs, frozen Material/MaterialPricingProfile/SavedItem/PricingComponent values, frozen Shop+category defaults, formula version, integer-cent cost breakdown/labor/overhead/suggested/manual/final price, warnings, accountability, lineage) that survives later edits to the LIVE Material/Profile/SavedItem/Component/Defaults records (verified directly — editing all four live records after the fact leaves the stored record byte-identical). Only `status`/`status_changed_at`/`superseded_by_snapshot_id` are ever updated post-insert (`active→superseded` on recalculation-accept, never on a non-persisted preview); old records are never deleted or mutated. `order_pricing.calculate_for_references` gained 4 purely-additive result keys (`material_profile_used`, `pricing_components_used`, `saved_item_used`, `category_defaults_used`) so `pricing_snapshot.build_calculated_snapshot` can freeze them. Hooked into `quotes.py`/`orders.py` item create + pricing-affecting update (create-snapshot on every add; on update only when pricing was actually re-resolved or the final price changed — pure description/notes edits create nothing), and into `quote_conversion.py` (Quote→Order conversion clones the lineage cross-document via `previous_snapshot_id`, zero recalculation, original quote-side record untouched). New read-only, tenant-scoped `GET/POST /pricing/snapshots[...]` endpoints: list by source, get-by-id, deterministic `/explain` (inputs/defaults/materials&components/cost calc/suggested-price calc/manual adjustments/final-price reason/assumptions&warnings/accountability/changes-from-previous — built purely from stored fields, zero AI), and `/compare` (material/labor/overhead/suggested/manual/final-price/margin deltas + changed-defaults/materials/components/warnings/formula-version flags). Provider-neutral **Advisory contracts** (`models/pricing_advisory.py`, `services/pricing_advisory.py`, `routers/pricing_advisory.py`) for the 9 EC16/EC17-future advisory types (AI pricing/historical/local-market/regional-market/target-margin/cost-risk/underpricing/overpricing/price-confidence analysis) — `POST /pricing/advisory/requests` always returns every requested type with `status="unavailable"`, `source_type="none"` (zero live AI/web/market calls anywhere in this phase, no provider name appears in any model); `/decision` records accept/reject without ever touching pricing on its own — the ONLY path that can create a new (accepted-status) snapshot requires a response that already carries real recommended-price data, which nothing in this phase can produce (exercised directly in tests via a synthetic populated fixture to prove the wiring exists without building fake operational AI). 19 new targeted tests (`tests/test_ec9_phase9g_snapshots_and_advisory.py`) + 65 directly-affected pre-existing tests (Phase 9F, 9A material-profiles/components/saved-items, 9E-4) all green — zero regressions. Per explicit owner directive, `testing_agent`, full backend/frontend regression, and browser automation were NOT run for this phase; Phase 9H was not started.

- **EC9 Phase 9E-4 — Services + Custom / Miscellaneous Pricing (2026-07):** `pricing_services.py` (a pre-existing, unmodified EC09-aligned formula set — labor/travel/trip/equipment/subcontract/permit/rush/minimum-charge across 17 service types x 8 pricing methods: hourly/per_crew_hour/per_unit/flat_fee/cost_plus/pass_through/hybrid/manual) was **wired into the `calculate_pricing()` dispatcher for the first time** (previously dead code). Added the **optional Labor Role override** (design/production/installer/helper/project_manager/admin/specialty_technician) — selecting a role swaps in that role's configured shop rate in place of the service_type preset rate (still beatable by an explicit hourly-rate override); `helper`/`specialty_technician` have no EC09 rate and are seeded at $0.00 (never invented) — selecting either while unconfigured surfaces a `calculation_warnings` entry and prices that portion at $0 rather than silently substituting another rate; manual pricing remains fully available regardless. New `pricing_custom.py` implements the EC09 §4 **strict Custom/Miscellaneous fallback**: `unit_price x quantity` only, no automated cost-estimation engine; an optional `unit_cost_manual` feeds profit/margin display ONLY and never changes price; an optional `markup_percent_adjustment` is a purely informational reference figure (never auto-applied, never switches the category into a cost-plus engine — both the entered unit price and the adjusted reference are returned separately and clearly labeled); the category's configured minimum charge applies only when it exceeds the `unit price x quantity` subtotal; the universal manual-override stays a fully separate, higher-priority path. This dispatch change made "custom" the sole remaining consumer of the old generic per-sqft/cost-plus fallback in `pricing.py`, which didn't match the EC09 spec for Custom anyway — that ~160-line dead-after-routing block (plus its now-unused `_to_dec`/`_round2` helpers) was removed. Frontend: new "Services" progressive-disclosure block (service type -> pricing-method-conditional fields for hours/crew/complexity, per-unit rate, flat fee, materials, equipment, design add-on, setup fee, travel, trip charge, outsourced/vendor pass-through, permit, rush, minimum override, Labor Role) and a deliberately minimal "Custom" block (item name, description, unit price, optional unit cost, optional minimum override, notes) in `CategorySpecificFields.jsx`; both categories added to `PricingCalculatorPage.jsx`'s dimensionless-category list (no width/height fields shown). The pre-existing generic "Save this as a reusable item" / manual-override controls on the Calculator page already work for any category, including these two — no duplicate controls were added. Two new tenant-editable global shop rates (`helper_hourly_rate`, `specialty_technician_hourly_rate`, both seeded $0.00) added to `starter_defaults.SHOP_DEFAULTS` + `ShopDefaultsIn` + the Pricing Foundation shop-rates list. 32 new targeted pytest tests (`tests/test_ec9_phase9e4_services_and_custom.py`) + a live curl E2E check against the real deployed API, all green; zero regressions across the 111 pre-existing EC9 pricing tests (9A–9E-3) that share `pricing.py`. Per explicit owner directive, `testing_agent` and the full regression suite were NOT run for this phase.

- **EC9 Phases 9A–9D — Pricing Foundation, Calculators & Order Pricing (2026-07):** `MaterialPricingProfile` (1:1 link to canonical EC7 `Material`, pricing-only fields — never a second inventory system), `PricingComponent` (non-inventory fees: setup/design/rush/permit/etc., closed `charge_type` enum), `PricingSavedItem` (reusable/common items with `quantity_tiers`, `material_refs`, `pricing_component_refs`, save-as-new/update/save-as-variation, tenant-scoped, archive/restore via `active`). Shop Defaults + per-category setup wizard (Phase 9B) with immutable effective-date snapshots. Phase 9C — Simplified Grouped Pricing Setup Quiz (`PricingQuizSubmission`, additive alongside the detailed wizard, pure-math derivation from a representative-job scenario, provisional suggestions requiring explicit owner review/apply, `draft→applied`/`draft→skipped` locked lifecycle, field-level validation reusing `ShopDefaultsIn`, allowlisted to only the fields the quiz itself suggested). Phase 9D — Materials & Saved Items UI: browse/search/edit Materials + Pricing Profiles, Pricing Components CRUD, Saved/Common Items library (quick-select filter, save-as-variation never mutates source), preloaded Business Card starter tiers (`Standard Paper Business Cards`, `Magnetic Business Cards`) with exact-match tier-price lookup (never invents a price for a non-configured quantity), reusable `MaterialSelector`/`SavedItemSelector`/`PricingComponentSelector` components (with inline "Add New" shortcuts) prepared for Phase 9E/9F integration, and Pricing Calculator wired to one-time/save-new/update-existing/save-as-variation saved-item flows. 50 targeted backend pytest tests across Phase 9A–9D, all green; frontend compiles clean.

- **Foundation Hardening (2026-07)** — fixes for 6 issues from an external code review of commit `8fbed3b`, approved by the owner (chose **shop_slug + email + password login** as the MVP fix for the tenant-login ambiguity, per the reviewer's own recommendation):
  1. **Tenant-scoped login/reset** — `POST /auth/login` and `POST /auth/request-password-reset` now require `tenant_slug` (email is only unique per-tenant, never globally) — logging in with the wrong shop for a shared email now fails 401 instead of silently resolving to an arbitrary tenant.
  2. **Clean frontend build** — `yarn build` was failing (`Cannot find module 'ajv/dist/compile/codegen'`) due to an unpinned transitive `ajv` v6/v8 hoisting conflict. Fixed by declaring an explicit `ajv: ^8.17.1` devDependency (forces a consistent v8 at the root); verified with a from-scratch `rm -rf node_modules && yarn install && CI=true yarn build` in a scratch directory. `yarn.lock` is now present and current (previously existed on disk but was never committed — will be picked up on the next "Save to GitHub"). Two pre-existing `react-hooks/exhaustive-deps` warnings (`TeamSchedulePage.jsx`, `PortalInvoicePayPage.jsx`) fixed so `CI=true` builds (as GitHub Actions uses) pass cleanly. Added `.github/workflows/ci.yml` (backend pytest + frontend jest + production build, 3 jobs) and a real `README.md` (setup, env vars, test commands, repo layout).
  3. **Vendor Materials route shadowing** — `GET/POST /vendors/materials` and `POST /vendors/seed/test-adapter` were registered *after* the dynamic `GET/PATCH /vendors/{vid}` routes, so FastAPI matched `/vendors/materials` as `{vid}="materials"` and always 404'd. Fixed by moving the fixed-path routes before the dynamic ones.
  4. **Password reset hardening** — reset tokens are now stored as a SHA-256 hash (`token_hash`), never plaintext; the raw token is only ever emailed (and, in `ENV=development` only, echoed back inline as `dev_reset_token` for local testing — this dev-only field is absent in production). The response is now byte-identical (`{"ok": true}`) whether or not the shop/email combination exists, closing an account-enumeration gap. Added a simple rate limit (max 5 reset requests per user per 15 minutes; over-limit requests still return the same generic response). Google sign-in's account-linking-by-email step now rejects (409) when the email exists in more than one tenant instead of silently picking one.
  5. **Grouped quiz validation + lifecycle lock** — `apply` now validates `accepted_shop_defaults` through the same `ShopDefaultsIn` Pydantic model (ranges, types) used by manual Pricing Foundation edits, and rejects any field the quiz itself didn't suggest. Status transitions are now locked to `draft→applied` XOR `draft→skipped` — re-applying, applying a skipped submission, skipping an applied submission, etc. all now fail with 400.
  6. **Quiz "Review later" now genuinely resumable** — a new "Continue previous setup" panel on the Pricing Foundation page lists draft quiz submissions with a "Resume" button that reopens the quiz dialog directly at the review/apply screen (skipping re-answering), instead of the button silently discarding local state.
  - 8 new targeted pytest tests (`test_foundation_hardening.py`) + 6 new live-server E2E pytest tests (`test_live_foundation_hardening.py`, added by the testing agent) — all passing. Full regression: 349/349 pre-existing backend tests + 25/25 frontend Jest tests unaffected. Verified end-to-end via `testing_agent_v4` (backend + frontend), zero bugs found.

## EC9 Phase 9H — Review, Validation, Documentation, and Closure (2026-07) — **EC9 FORMALLY CLOSED**

**1. Implementation inventory.** Models: `material.py`/`material_pricing_profile.py` (EC7 canonical Material + EC9 pricing profile), `pricing_component.py`, `pricing_saved_item.py`, `pricing_settings.py` (Shop Defaults + category defaults), `pricing_quiz_submission.py`, `pricing_snapshot_record.py`, `pricing_advisory.py`. Services: `pricing.py` (dispatcher/`calculate_pricing`), `pricing_flat_sqft.py`, `pricing_apparel.py`, `pricing_promotional.py`, `pricing_vehicle_graphics.py`, `pricing_services.py`, `pricing_custom.py`, `pricing_materials.py`, `pricing_components.py` (service), `pricing_saved_items.py` (service), `pricing_quiz.py`, `pricing_snapshot.py` (embedded-dict builder), `pricing_snapshot_records.py` (immutable records), `pricing_advisory.py` (service), `order_pricing.py` (shared resolver), `starter_defaults.py` (single source of truth for the starter pack, `STARTER_DEFAULT_VERSION = "1.2.0"`). Routers (all registered in `server.py`, confirmed no shadowing/unregistered routes): `pricing.py` (`/pricing/settings*`, `/pricing/calculate`, `/pricing/categories`), `pricing_materials.py` (`/pricing/material-profiles`), `pricing_components.py` (`/pricing/components`), `pricing_saved_items.py` (`/pricing/saved-items`), `pricing_quiz.py` (`/pricing/quiz`), `pricing_snapshots.py` (`/pricing/snapshots`), `pricing_advisory.py` (`/pricing/advisory`), plus EC9 fields/endpoints inside `quotes.py`/`orders.py`. Frontend pages: `PricingFoundationPage.jsx`, `PricingCalculatorPage.jsx`. Frontend components: `CategorySetupWizard.jsx`+`wizardConfigs.js`, `GroupedPricingQuiz.jsx`, `CategorySpecificFields.jsx`, `MaterialPricingPanel.jsx`, `PricingComponentsPanel.jsx`, `SavedItemsPanel.jsx`, `components/pricing/selectors/{MaterialProfileSelector,PricingComponentSelector,SavedItemSelector}.jsx`, `components/commerce/LineItemDialog.jsx`. DB collections+indexes: `pricing_settings`, `materials`, `material_pricing_profiles`, `pricing_components`, `pricing_saved_items`, `pricing_quiz_submissions`, `pricing_snapshot_records`, `pricing_advisory_requests` (all tenant-scoped indexed). Test files (15): `test_ec9_phase9a_invariants.py`, `test_ec9_phase9b_global_foundation.py`, `test_ec9_phase9c_grouped_quiz.py`, `test_ec9_phase9d_materials_and_saved_items.py`, `test_ec9_material_pricing_profiles.py`, `test_ec9_pricing_components.py`, `test_ec9_pricing_saved_items.py`, `test_ec9_phase9e1_flat_sqft_categories.py`, `test_ec9_phase9e2_apparel_promo.py`, `test_ec9_phase9e2_corrections_and_9e3_vehicle.py`, `test_ec9_phase9e4_services_and_custom.py`, `test_ec9_phase9f_quote_order_integration.py`, `test_ec9_phase9g_snapshots_and_advisory.py`, `test_ec9_phase9h_closure_regressions.py` (new), `test_pricing_snapshot.py`, `test_foundation_hardening.py` — all included in normal `pytest tests/` discovery. Category IDs (9): `banners, rigid_signs, cut_vinyl, digital_print, vehicle_graphics, apparel, services, promotional, custom`. Source labels in active use: `shop_default, saved_item, user_entered, grouped_quiz, exact_table, foundation_estimate, calculator, manual, suggested` (the full EC09 §13 compatibility list — incl. `material_pricing_profile, pricing_component, AI_estimate, historical_data, market_data` — is contractually supported but not all emitted yet, correctly deferred to EC16/17). Snapshot statuses: `active, superseded, historical, candidate, accepted, rejected`. Advisory statuses: `unavailable, not_requested, pending, completed, partial, failed, stale, accepted, rejected`.

**Findings from the inventory:** one abandoned/duplicate frontend selector found and removed — `components/pricing/selectors/MaterialSelector.jsx` (a raw canonical-Material selector prepared in Phase 9D, zero usages anywhere; the actual profile-based integration point Phase 9F/9G built and wired everywhere is `MaterialProfileSelector.jsx`). No duplicate pricing engines, no disconnected routers, no unregistered EC9 routers, no shadowed routes found.

**2. Category coverage matrix** (Complete / Complete-with-approved-provisional-assumptions / Defect found / Deferred-by-spec):

| Category | Foundation defaults | Required/conditional inputs | Backend path | Suggested+manual | Source labels | Integer cents | Snapshot | Quote/Order/Item | Targeted tests | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| Banners | ✅ | ✅ width/height/finishing | `pricing_flat_sqft.py` | ✅ | ✅ | ✅ | ✅ | ✅ | 9E-1, 9H closure | **Complete** (9H fixed a dimension-forwarding defect in Quote/Order paths — now Complete) |
| Rigid Signs/Panels | ✅ | ✅ substrate/finish | `pricing_flat_sqft.py` | ✅ | ✅ | ✅ | ✅ | ✅ | 9E-1 | **Complete** |
| Digital Print | ✅ | ✅ media/lamination | `pricing_flat_sqft.py` | ✅ | ✅ | ✅ | ✅ | ✅ | 9E-1 | **Complete** |
| Cut Vinyl | ✅ | ✅ color-count/complexity | `pricing_flat_sqft.py` | ✅ | ✅ | ✅ | ✅ | ✅ | 9E-1, 9G | **Complete** |
| Apparel | ✅ | ✅ decoration method/area/qty-tier | `pricing_apparel.py` | ✅ | ✅ | ✅ | ✅ | ✅ | 9E-2 | **Complete with approved provisional assumptions** (decoration-area estimate method — see register below) |
| Promotional Items (incl. Business Cards) | ✅ | ✅ saved-item tier / manual fields | `pricing_promotional.py` | ✅ | ✅ | ✅ | ✅ | ✅ | 9E-2, 9D | **Complete** — exact-tier lookup never invents a price for a non-configured quantity (verified: manual-price-required state, no fabricated number) |
| Standard Paper Business Cards | ✅ (preloaded `PricingSavedItem`) | ✅ quantity tiers | `pricing_promotional.py` (via saved item) | ✅ | ✅ | ✅ | ✅ | ✅ | 9D, 9E-2 | **Complete** — this is a Saved Item preset under `promotional`, not a distinct category ID (matches EC09 data model; not a gap) |
| Magnetic Business Cards | ✅ (preloaded `PricingSavedItem`) | ✅ quantity tiers | `pricing_promotional.py` (via saved item) | ✅ | ✅ | ✅ | ✅ | ✅ | 9D, 9E-2 | **Complete** (same note as above) |
| Vehicle Graphics/Wraps | ✅ | ✅ vehicle type/coverage | `pricing_vehicle_graphics.py` | ✅ | ✅ | ✅ | ✅ | ✅ | 9E-3 | **Complete with approved provisional assumptions** (vehicle base-sqft/install-hours for types without an exact EC09 value — see register below) |
| Services | ✅ | ✅ service_type/method/Labor Role | `pricing_services.py` | ✅ | ✅ | ✅ | ✅ | ✅ | 9E-4 | **Complete with approved provisional assumptions** (helper/specialty_technician rates seeded $0 — see register below) |
| Custom/Miscellaneous | ✅ | ✅ unit price/qty (strict manual fallback, no cost engine per EC09 §4) | `pricing_custom.py` | ✅ (manual is the ONLY suggested-price source by spec) | ✅ | ✅ | ✅ | ✅ | 9E-4 | **Complete** — strict-manual behavior is EC09-specified, not a gap |

No required behavior in the matrix above is labeled "future enhancement" — every ✅ is implemented and tested today; the only non-Complete rows carry approved, warned, editable, source-labeled, snapshotted provisional starter values (see register below), which the closure criteria explicitly permit.

**3. Provisional assumption register:**

| Assumption | Category | Current starter value | Why provisional | Tenant-editable | Warning shown | Source label | Snapshot behavior | Owner review needed later |
|---|---|---|---|---|---|---|---|---|
| Decoration-area / method cost estimate (no exact uploaded price table per decoration method x area) | Apparel | Formula-derived from area + method multiplier in `pricing_apparel.py` | EC09 didn't supply an exact per-method price table | Yes (shop defaults + category defaults) | Yes (`calculation_warnings`) | `foundation_estimate` | Frozen in `PricingSnapshotRecord.category_defaults_used`/`material_values_used` at calc time | Yes — replace with exact table if/when owner supplies one |
| Vehicle type base-sqft / install-hours for types without an exact EC09 value (`mini_van`, `box_truck_24`, `semi`, `other`) | Vehicle Graphics | Seeded estimate in `starter_defaults.CATEGORY_DEFAULTS["vehicle_graphics"]` | EC09 gave exact figures for common types only | Yes | Yes | `foundation_estimate` | Frozen per-snapshot | Yes |
| `helper_hourly_rate`, `specialty_technician_hourly_rate` | Services | `$0.00` | No EC09-approved rate exists; never invented | Yes (Shop Defaults) | Yes — selecting either role while unconfigured surfaces a `calculation_warnings` entry and prices at $0 rather than substituting a different rate | `shop_default` | Frozen per-snapshot | Yes — owner must set real rates before relying on these roles |
| Any other starter default seeded at $0 pending owner-approved value | (n/a — none found beyond the two above; all other `SHOP_DEFAULTS`/`CATEGORY_DEFAULTS` fields carry an EC09-sourced non-zero starter value) | — | — | — | — | — | — | — |

EC9 closure is **not blocked** by this register — every entry is warned, editable, source-labeled, and correctly frozen into every snapshot that uses it, exactly per the closure criteria.

**4. Terminology guard:** `python -m app.core.terminology_guard /app` → `OK`. Canonical terms (Quote/Order/Order Item/Work Order/Work Order Summary/Webstores/Wrap Lab) enforced; "Job Ticket" never introduced as user-facing terminology anywhere in EC9 code.

**5. Files changed during Phase 9H:**
- **Fixed (critical defect):** `routers/quotes.py`, `routers/orders.py` — `_resolve_item_pricing` now accepts + forwards `width_inches`/`height_inches` at all 3 call sites each (add/update/recalculate-preview); `RecalculatePreviewIn` gained optional `width_inches`/`height_inches` overrides; `pricing_trigger_fields` now includes both dimension fields so a dimensions-only edit also triggers recalculation.
- **Removed (dead code):** `components/pricing/selectors/MaterialSelector.jsx` (zero usages, superseded by `MaterialProfileSelector.jsx`).
- **New (regression lock):** `tests/test_ec9_phase9h_closure_regressions.py` (5 tests).
- **Documentation:** `PRD.md`, `progress_register.md`, `checkpoint_reference_table.md`, `permanent_backlog_register.md`, `preflight/EC9_..._PREFLIGHT.md` (closure record appended).

**6. Defects found:** 1 critical (dimension-forwarding bug above, via `testing_agent_v4`'s Vehicle Graphics/Banners workflow test). **Defects fixed:** 1/1. **Deferred findings:** none — the testing agent's other observations were confirmations of correct-by-spec behavior (advisory always "unavailable", business-card non-matching-quantity requires manual price), not defects.

**7. Test results:** EC9-targeted (all phases together, 15 files): **196/196 passed**. Phase 9H closure regressions: **5/5 passed**. Full backend regression (`pytest tests/`, with `REACT_APP_BACKEND_URL` exported so the 2 live-URL test files collect): **537 passed, 0 failed** (2 pre-existing collection-only quirks confirmed to fully pass, not an EC9 or app defect). Frontend Jest: **25/25 passed**. Frontend production build (`yarn build`): **clean**. Terminology guard: **OK**. `testing_agent_v4` EC9 closure workflow pass: **1 defect found + fixed** (dimension-forwarding), 0 other defects, all 25 representative user workflows (Foundation setup, grouped quiz, wizard, Materials/Components/Saved Items CRUD + save-as-variation, business-card exact/non-exact tier, all 9 category calculators, manual-vs-suggested preservation, Quote→Order conversion losslessness, recalculation preview/accept/reject, snapshot explain/compare, advisory-never-changes-pricing) confirmed working.

**8. Security/data-integrity checks (EC9-specific, all confirmed):** tenant isolation and permission enforcement on every EC9 endpoint (incl. new Phase 9G `/pricing/snapshots`/`/pricing/advisory` — cross-tenant snapshot/advisory access returns 404, unknown role returns 403); cross-tenant Material/Saved Item/Pricing Component references are rejected by `resolve_references` (404, tenant-scoped lookup); archived Materials/Saved Items excluded from active selector queries; locked/non-draft Quotes/Orders reject `recalculate-preview` (400); manual pricing cannot spoof backend-authoritative suggested pricing (`unit_price_cents` is always server-recomputed when `selected_price_source="suggested"`); snapshot pricing evidence is append-only/immutable (only lineage metadata fields ever change post-insert, verified by directly editing live Material/Profile/SavedItem/Component records after calc and re-fetching the historical record byte-identical); money is integer cents everywhere; advisory failures/"unavailable" never block normal pricing (verified — creating/deciding an advisory request never touches any Quote/Order/Order Item unless a synthetic, already-populated response is explicitly accepted, which no live code path can produce this phase).

**9. Snapshot validation (all confirmed via `test_ec9_phase9g_snapshots_and_advisory.py` + `test_pricing_snapshot.py`):** every priced Quote/Order item creates an immutable snapshot record; live source-record edits never alter a historical record; recalculation creates new lineage (`previous_snapshot_id`/`superseded_by_snapshot_id`) while the old record's pricing data is untouched; a rejected/non-accepted preview persists nothing; an accepted recalculation supersedes the prior active record correctly; Quote→Order conversion is lossless and cross-document lineage-linked; `/explain` and `/compare` are deterministic (byte-identical on repeat calls); `formula_version`/`starter_default_version` are frozen per-record.

**10. Advisory validation (all confirmed):** zero AI/web/market provider SDK or network call exists anywhere in `services/pricing_advisory.py`; every response this phase creates is `status="unavailable"`, `source_type="none"`; no fabricated recommendation is ever returned; records are tenant-scoped; the response contract validates partial results (invalid requested types silently dropped, not rejected); a "rejected" decision never touches pricing; an "accepted" decision only creates a snapshot given a response that already carries real price data (unreachable via any live code path this phase — exercised only via a synthetic test fixture); no operational "Get AI pricing advice" button exists anywhere in the UI.

**11. Permanent EC9 architecture decisions (recorded for all future checkpoints):**
- EC7 `Material` remains the single canonical physical-material record; `MaterialPricingProfile` holds pricing-only data 1:1 against it — never a second inventory system.
- `PricingComponent` represents non-inventory charges (fees), never inventory.
- `PricingSavedItem` references canonical Material IDs, never duplicates material data.
- Backend (`calculate_pricing()` + `order_pricing.py`) is the sole pricing authority; the frontend is a dumb form.
- All money persists in integer cents everywhere.
- Manual and suggested prices are always stored separately (`manual_price_cents` / `suggested_price_cents`); `selected_price_source` records which one is in effect.
- One shared calculation pipeline (`calculate_pricing()`) dispatches to category-specific formula modules — never a parallel/duplicate pricing engine.
- Historical pricing snapshots (`PricingSnapshotRecord`) are append-only; only lineage/status metadata is ever updated post-insert.
- Recalculation always requires an explicit accept action (preview is pure compute, zero persistence, until accepted).
- Quote→Order conversion never automatically recalculates — it clones the snapshot lineage losslessly.
- Advisory analysis is optional, provider-neutral, and can never block or silently alter pricing; owner must explicitly accept any advisory-driven adjustment.
- Live AI/market-data integration is explicitly deferred to EC16/EC17 — no operational AI exists in EC9.
- Every provisional starter-default assumption must be warned, tenant-editable, source-labeled, and frozen into every snapshot that uses it.

**12. EC9 final status: CLOSED.** All closure criteria met: every required category path implemented; all 196 EC9-targeted + 5 closure-regression tests pass; full backend regression passes (537/537, with the 2 documented unrelated pre-existing collection quirks resolved by env var); frontend Jest passes (25/25); frontend production build passes; terminology guard passes; `testing_agent_v4` completed with its 1 found defect fixed and verified; no unresolved pricing-data-corruption, tenant-isolation, silent-recalculation, or mutable-historical-evidence risk remains; documentation updated across all 4 tracking files + the EC9 preflight closure record. **Current EC10/EC11 status is tracked below: EC10 is closed; EC11 is in progress with Phase 11A complete.**



- Backend: `cd /app/backend && python -m pytest tests/ -q` (with `REACT_APP_BACKEND_URL` exported) → **537 passed, 0 failed** (2026-07, EC9 Phase 9H full-regression closure run; 2 pre-existing collection-only files require the env var to be exported — confirmed to pass fully when it is, not an EC9 or app defect). EC9-targeted-only run (15 files, all phases): **196 passed**. Phase 9H closure-regression file (`test_ec9_phase9h_closure_regressions.py`): **5 passed**.
- Frontend Jest: `cd /app/frontend && CI=true yarn test --watchAll=false` → **6 suites / 25 tests passed**.
- Frontend production build: `cd /app/frontend && yarn install --frozen-lockfile && yarn build` → verified clean from a completely fresh `node_modules` (2026-07 Foundation Hardening pass).
- CI: `.github/workflows/ci.yml` runs backend pytest + frontend jest + frontend production build on every push/PR.
- Frontend E2E: `testing_agent_v4` (2026-07 EC9 Phase 9H closure pass) → 25 representative workflows tested end-to-end (Foundation setup, grouped quiz, wizard, Materials/Components/Saved Items CRUD, all 9 category calculators, manual-vs-suggested, Quote→Order conversion, recalculation accept/reject, snapshot explain/compare, advisory-never-changes-pricing) — **1 critical defect found (Quote/Order item width/height not reaching the pricing engine) and fixed same-session**, 0 other defects. Prior: `testing_agent_v4` (2026-07 Foundation Hardening pass) → **100% pass, 0 bugs**.
- Prior: `testing_agent_v3_fork` iteration 10 (EC7 closure) → PASS. Iteration 9 (EC6): 100% pass. Iteration 8 (EC5): 100% pass.

## Test credentials

`AUTH_DEV_BYPASS=true` in development; dev-bypass banner visible in UI. No production credentials exist in `.env`.

## Priority backlog (P0/P1/P2)

**2026-02 update:** the detailed, checkpoint-by-checkpoint backlog now lives in `/app/memory/permanent_backlog_register.md` (committed permanent scope) and `/app/memory/checkpoint_reference_table.md` (status + holds per checkpoint). This section is kept short and points there instead of duplicating it.

### P0 — Immediate next checkpoint
- EC9 Phases 9A–9D COMPLETE. Foundation Hardening COMPLETE. Phase 9E-1/9E-2/9E-3/9E-4 COMPLETE. Phase 9F COMPLETE. Phase 9G COMPLETE. Phase 9H (Review, Validation, Documentation, Closure) COMPLETE (2026-07). **EC9 is fully CLOSED — owner formally accepted closure 2026-02.**
- **EC10 Phase 10B (Quick and Detailed Internal Intake) — COMPLETE (2026-02).** Staff-only `/intake`, `/intake/new`, `/intake/:id` pages (list with status/priority/search filters + missing-info badges; Quick↔Detailed toggle on the same form state; multi-item add/edit/duplicate/reorder/remove; file & questionnaire-reference attachment via existing `/files` system; backend-controlled status-transition buttons + reason-required reject/cancel dialog + status-history timeline; read-only conversion-preview panel). Backend additions: 7-field additive pricing-workflow contract on `IntakeItem` (`pricing_status`/`pricing_snapshot_id`/`selected_price_source`/`manual_price_cents`/`pricing_warning_codes`/`pricing_ready`/`pricing_notes` — reference-only, never calculates), `status_history`/`reviewed_by_user_id` on `IntakeSubmission`, item CRUD endpoints (`PATCH`/`DELETE`/`duplicate`/`reorder`), submit-readiness validation (`missing_information_for_submission()`), list search/filters. 57/57 backend tests green (15 new Phase 10B + 15 re-verified Phase 10A + 27 directly-affected pre-existing). `yarn build` compiles cleanly. No `testing_agent` run (not authorized) — 2 sanity screenshots taken as a minor deviation, documented in the completion report. Full report: `/app/evidence/EC10_PHASE10B_COMPLETION_REPORT.md`.
- **EC10 Phase 10C (Asset Upload and Visual Markup) — COMPLETE (2026-02).** New `VisualMarkup`/`MarkupVersion` models (append-only version history, `source_file_id` immutable), `markup_service.py`, `/api/markup*` staff-only router. Frontend: `MarkupWorkspace`/`MarkupCanvas`/`MarkupToolbar`/`MarkupVersionHistory` (Fabric.js `6.9.1` + `pdfjs-dist` `5.6.205`), wired into `FileAttachmentPicker.jsx`'s intake file chips. 10 annotation tools (select/draw/line/arrow/rect/ellipse/text/callout/pin/highlight); allowlisted-object + embedded-binary + size/count validation server-side; separate rendered-preview file per version (never the source); coordinate/resize contract (fixed logical canvas resolution, display-time-only zoom scaling); Intake/Intake Item attach wiring; full audit trail (never logs raw markup JSON). 12/12 targeted backend tests green (`tests/test_ec10_phase10c_visual_markup.py`); `yarn craco build` compiles cleanly. No `testing_agent` run (not authorized). Full report: `/app/evidence/EC10_PHASE10C_COMPLETION_REPORT.md`. **Phase 10D implementation requires explicit owner authorization — not yet given.** 3 owner decisions remain open (unchanged from Phase 10A/10B — see completion report §14).
- **EC10 Phase 10D (Customer Decision Room — internal authoring only) — COMPLETE (2026-02).** New `DecisionRoom` (embedded `DecisionOption` list, mirrors `IntakeItem` precedent) + append-only `DecisionRoomVersion` (mirrors `MarkupVersion`) models, `decision_room_service.py`, `/api/decision-rooms*` staff-only router (`decision_room:read`/`write` granted to all staff; `decision_room:publish`/`archive` owner/admin-only). Full authoring surface: multi-option CRUD, duplicate (never inherits Recommended badge), reorder, archive/restore, at-most-one-Recommended enforcement, media/proof/markup/pricing-snapshot attach-detach (all cross-tenant-validated, all money integer cents, pricing never recalculated — only copied from an EC9 `PricingSnapshotRecord` or human-typed manual value), readiness validation report, draft→ready→published lifecycle (`published` reachable ONLY via a dedicated `/publish` action that freezes an immutable `DecisionRoomVersion`; edits after publish bump `current_version` without a new frozen row until the next explicit publish), internal customer-safe preview endpoint (strips internal_notes/cost/margin/employee-ids), full audit trail. Frontend: `/decision-rooms`, `/decision-rooms/new`, `/decision-rooms/:id` (list, create, editor with `DecisionOptionCard`/`DecisionOptionPricingSection`/`DecisionOptionMediaSection`/`DecisionRoomVersionHistory`/`DecisionRoomPreviewDialog`), nav entry under Shop Operations. **No customer/public access, no decision-to-order integration, no templates — all explicitly deferred.** 19/19 new targeted backend tests green (`tests/test_ec10_phase10d_decision_room.py`) + 68/68 directly-affected prerequisite tests (10A/10B/10C/9G/permissions) re-verified green; `yarn craco build` compiles cleanly. No `testing_agent`, no full regression, no browser automation used (per owner instruction). Full report: `/app/evidence/EC10_PHASE10D_COMPLETION_REPORT.md`. **Phase 10E implementation requires explicit owner authorization — not yet given.** 3 owner decisions remain open (unchanged — see completion report §18).
- **EC10 Phase 10E — split into 5 controlled subphases per owner instruction (10E-1 Customer-safe access/display; 10E-2 selection/rejection/change requests; 10E-3 questions/comments/pins/save-for-later; 10E-4 internal review queue/proof references; 10E-5 validation/closure).**
- **EC10 Phase 10E-1 (Customer-safe access and published room display) — COMPLETE (2026-02), including the required completion-gap fix for customer-safe derivative media (2026-02).** Two read-only access modes both resolving through a new shared `decision_room_service.get_customer_view()` (renders ONLY the frozen `DecisionRoomVersion`, never the live draft): (1) **Customer Portal** `GET /portal/decision-rooms[/{id}]`, new `portal:view_decision_rooms` permission (added to `approver_only`/`owner_full` presets); (2) **Public Token** `GET /public/decision-rooms/{id}?t=...`, new `decision_room_view` token action (multi-use), plus staff `POST /decision-rooms/{id}/share` mint + `DELETE /decision-rooms/share-tokens/{id}` revoke (mirrors `documents_meta.py`'s existing share pattern). Customer-safe filtering reuses Phase 10D's `_option_preview()` (strips internal fields, excludes inactive options). `published`/`closed`/`expired` rooms remain viewable (200, historical-record framing); `draft`/`ready`/`archived` → 404 (never leaks existence); revoked/expired/wrong-purpose/invalid tokens → 410/403/401 (reuses existing `resolve_public_token()` unchanged). **Media gap fix:** added `GET /portal/decision-rooms/{id}/media/{file_id}` + `GET /public/decision-rooms/{id}/media/{file_id}` (reuses the existing `storage.get_bytes()` proxy pattern from `documents.py`) — serves only media referenced in the FROZEN published `options_snapshot` (never the live option list), requiring `FileRecord.visibility == "customer_visible"` for plain `file_ids` attachments while treating `rendered_preview_file_id`/`thumbnail_file_id`/a newly-added frozen `_frozen_proof_preview_file_id` (Proof preview, eagerly resolved AT PUBLISH TIME so a later Proof re-version can't retroactively change what was shown) as structurally customer-safe. Frontend: shared `DecisionRoomCustomerView.jsx` + new `DecisionRoomMedia.jsx` (blob-fetch, image vs. PDF-link fallback, safe unavailable placeholder), used by both the Portal detail page and the Public token page. 14/14 new targeted tests green across both Phase 10E-1 sessions (`tests/test_ec10_phase10e1_decision_room_customer_access.py` 9/9 + `tests/test_ec10_phase10e1b_decision_room_customer_media.py` 5/5) + 19/19 Phase 10D re-verified; `yarn craco build` compiles cleanly. No `testing_agent`, no full regression, no browser automation/screenshots (per owner instruction). Full reports: `/app/evidence/EC10_PHASE10E1_COMPLETION_REPORT.md`, `/app/evidence/EC10_PHASE10E1_MEDIA_FIX_COMPLETION_REPORT.md`.
- **EC10 Phase 10E-2 (Customer Option Selection, Rejection, and Change Requests) — COMPLETE (2026-02).** New append-only `CustomerDecision` model (`models/decision_room.py`) with the owner's exact `action_type` literals — `option_selected`/`option_rejected`/`all_options_rejected`/`change_requested` (no abbreviated values; `save_for_later` explicitly excluded, deferred to Phase 10E-3) — tied to the authoritative `published_version_id` (the exact frozen `DecisionRoomVersion._id`, not just a version number). New room-level `allow_reject_all` flag (frozen at publish, defaults `False` — never assumed enabled). Routes: `POST/GET /portal/decision-rooms/{id}/decisions` (new `portal:respond_decision_rooms` permission, distinct from view), `POST/GET /public/decision-rooms/{id}/decisions?t=...` (reuses the existing multi-use `decision_room_view` token, never consumed), staff-only READ-ONLY `GET /decision-rooms/{id}/decisions` + `POST .../decisions/{id}/acknowledge` (flips `internal_review_status` only — staff can never accept/apply/change pricing here, that stays Phase 10F). Enforces: room must be `published` to accept new writes (closed/expired stay readable, reject with 400; unpublished/archived 404, never leaking existence); option must exist+be active on the exact frozen snapshot; idempotency via a unique sparse `(tenant_id, decision_room_id, idempotency_key)` index (duplicate clicks never duplicate rows, race-safe); selecting a different option supersedes the same actor's prior unresolved selection via `supersedes_decision_id` (prior row never mutated — full history preserved); notifications reuse the existing `notify_tenant_owners()` contract as-is, wrapped so a failure never loses the already-saved decision. Frontend: `DecisionRoomCustomerView.jsx` gains Select/Reject/Reject-all/Request-change controls (all hidden once the room leaves `published`), new staff-only `DecisionRoomCustomerDecisionsPanel.jsx` (view + acknowledge only, mounted on the room editor). 12/12 new targeted tests green (`tests/test_ec10_phase10e2_decision_room_customer_decisions.py`, including an explicit before/after `db.orders` equality assertion proving zero commercial mutation) + 10D/10E-1 decision-room tests re-verified green; `yarn craco build` compiles cleanly. No `testing_agent`, no full regression, no browser automation/screenshots (per owner instruction). Full report: `/app/evidence/EC10_PHASE10E2_COMPLETION_REPORT.md`.
- **EC10 Phase 10E-3 (Customer Questions, Anchored Comments/Pins, and Save for Later) — COMPLETE (2026-02).** Three new, deliberately SEPARATE models (`DecisionRoomQuestion`, `DecisionRoomOverlay`, `SavedForLater`) — kept apart from `CustomerDecision` per owner instruction not to distort its selection/rejection history. Questions: `open→answered→resolved` lifecycle, gated by the dormant Phase 10D `allow_customer_questions` flag (now live), optional anchor to an option/`source_file_id`/`visual_markup_id`/`markup_version_id`/`page_number`, staff `respond`/`resolve` endpoints (one response per question, no threading), customer-safe projector strips `responded_by_user_id`/identity fields. Anchored overlays: `comment`|`pin` types with normalized (0.0-1.0) `normalized_x`/`normalized_y` coordinates, gated by the dormant `allow_customer_comments` flag (now live), stored ENTIRELY separately from staff `MarkupVersion.structured_markup_json` (verified untouched by test), server-assigned sequential `marker_number` for pins, customer-only `edit`/`withdraw` of their own `active` overlay (append-only — withdrawn rows locked, never deleted), Fabric.js JSON hard-rejected via `ConfigDict(extra="forbid")` (422). Save for later: minimal separate model, gated by `allow_save_for_later`, never selects/rejects/prices/extends-expiration, stores the exact `published_version_id`. Every anchor (option/file/markup/page) validates against the frozen published version via a shared `_resolve_and_validate_anchor()` (reuses the exact Phase 10E-1 `resolve_customer_safe_media()` allowlist for files). Public-token endpoints rate-limited (20/60s/IP). **Bug found+fixed**: MongoDB compound `sparse=True` indexes don't exclude docs missing only SOME fields — replaced all 4 idempotency indexes (including the pre-existing Phase 10E-2 one) with `partialFilterExpression={"idempotency_key": {"$type":"string"}}` + a `_for_insert()` helper that drops falsy `idempotency_key` before insert; stale indexes on the dev DB dropped/recreated. Frontend: `DecisionRoomAnchorableMedia.jsx` (click-to-pin/comment over `image`-type option media, marker rendering), Questions/Save-for-later sections in `DecisionRoomCustomerView.jsx`, staff-only `DecisionRoomQuestionsPanel.jsx` (view/respond/resolve). 13/13 new targeted tests green (`tests/test_ec10_phase10e3_questions_overlays_save_for_later.py`) + 53/53 across all decision-room test files (10D+10E-1+10E-2+10E-3, confirming the index fix caused zero regression); `yarn craco build` compiles cleanly. No `testing_agent`, no full regression, no browser automation/screenshots (per owner instruction). Known gaps: PDF-page-anchored overlays validate end-to-end on the backend but have no click-to-place UI yet (image-only in this phase); no dedicated staff UI for viewing overlays (backend endpoint exists, tested); no customer-facing push notification for staff responses/saves (no reusable channel without building new infra, which is forbidden). Full report: `/app/evidence/EC10_PHASE10E3_COMPLETION_REPORT.md`.
- **EC10 Phase 10E-4 (Internal Review Queue and Proof References) — COMPLETE (2026-07-16).** Reused the existing Phase 10E-4 groundwork (`DecisionRoomReviewMeta`, `DecisionRoomInternalNote`, review metadata indexes, and `decision_room_service.py` queue/assignment/note/review helpers) without duplicating models, indexes, or queue storage. Added staff-only `/api/decision-room-review-queue` endpoints for normalized queue list/filter, assign/unassign, supported acknowledge/review actions, and internal notes. Queue items project customer decisions, questions, overlays, and save-for-later records into one internal triage view and enrich option-linked decisions with room title, customer name, option label, and proof id when present. Frontend: new compact `/decision-room-review-queue` page and Shop Operations nav entry with filters, status pills, assign-to-me, note, and mark-reviewed controls. Added targeted test file `tests/test_ec10_phase10e4_decision_room_review_queue.py`; local targeted execution is blocked by no local MongoDB service, while `compileall`, backend collection, test-storage fallback checks, and frontend production build pass. Backend CI fixes included removing unavailable `emergentintegrations==0.2.0`, adding missing `pytest-asyncio==1.4.0`, adding an `ENV=test` object-storage fallback for unit tests, and making live URL-only tests skip cleanly when `REACT_APP_BACKEND_URL` is absent. Full report: `/app/evidence/EC10_PHASE10E4_COMPLETION_REPORT.md`. **Phase 10E-5 later completed Phase 10E closure; Phase 10F, Phase 10G, and final EC10 closure were completed afterward.**
- **EC10 Phase 10E-5 (Final Phase 10E Validation and Closure) — COMPLETE (2026-07-16); Phase 10E CLOSED.** Validated the complete Phase 10E system without adding new customer-facing features or duplicate tests: 10E-1 customer-safe portal/public-token access and frozen published-version display, 10E-1 media fix, 10E-2 append-only/idempotent customer decisions and superseding selections, 10E-3 separate questions/overlays/save-for-later flows, and 10E-4 normalized internal review queue. Reused GitHub Actions evidence from run `29480340192` at commit `1937dcf78d39095195dab8ebf3a6d2ec371c9159` (`backend-tests`, `frontend-tests`, `frontend-build` all passed) rather than rerunning duplicate backend pytest locally without MongoDB. Closure audit confirms no internal notes/costs/profit/margins/raw storage paths are exposed, customer media is limited to frozen published-version media, customer freehand/Fabric.js drawing is unavailable, staff responses/internal notes remain separate, and no customer/review action mutates Quote, Order, Order Item, Proof, price, pricing snapshot, or production data. Full report: `/app/evidence/EC10_PHASE10E_CLOSURE_REPORT.md`. **Phase 10F, Phase 10G, and final EC10 closure were completed afterward; EC10 is now CLOSED.**
- **EC10 Phase 10F (Decision-to-Quote/Order Integration) — COMPLETE (2026-07-16).** Added explicit staff-controlled `POST /api/decision-rooms/{room_id}/decisions/{decision_id}/apply`; only non-superseded `option_selected` customer decisions can mutate a linked Quote Line Item or Order Item, and only after staff applies them. Customer actions remain append-only/idempotent and never auto-apply. Applying a Decision Room option creates an immutable pricing snapshot record and rehydrates frozen EC9 snapshot data without recalculating from live defaults/materials/components. Frontend apply controls were added to the room decision panel and review queue. Targeted backend test file: `tests/test_ec10_phase10f_decision_apply.py`. Full report: `/app/evidence/EC10_PHASE10F_COMPLETION_REPORT.md`.
- **EC10 Phase 10G (EC10-scoped Templates) — COMPLETE (2026-07-16).** Added tenant-scoped `TemplateDefinition` (`template_definitions`) with `template:read`/`template:write`, `/api/templates` CRUD/archive/restore/apply, and compact `/templates` frontend page/nav entry. Template types are limited to `intake`, `questionnaire`, and `decision_options`; apply copies values at apply time so later template edits never mutate already-created intake records or Decision Room options. Explicitly deferred Quote/Order/Order Item templates, Proof templates, production workflow templates, appointment templates, and email/SMS templates to their owning future scopes. Targeted backend test file: `tests/test_ec10_phase10g_templates.py`. Full report: `/app/evidence/EC10_PHASE10G_COMPLETION_REPORT.md`.
- **EC10 Phase 10H (Final EC10 Validation and Closure) — COMPLETE (2026-07-16); EC10 CLOSED.** Local validation: `python -m compileall backend\app backend\tests\test_ec10_phase10f_decision_apply.py backend\tests\test_ec10_phase10g_templates.py` passed; frontend Jest passed (7 suites / 29 tests); frontend production build passed. Local targeted backend pytest was not runnable because the local Python runtimes do not include `pytest` and no local MongoDB-backed backend test environment is available; GitHub Actions remains the authoritative backend pytest environment. Closure report: `/app/evidence/EC10_CLOSURE_REPORT.md`. **EC11 later began with Phase 11A only.**
- **EC11 Phase 11A (Production Workflow Definitions and Canonical Stage Contracts) — COMPLETE (2026-07-16); EC11 IN PROGRESS.** Added tenant-scoped `ProductionWorkflowDefinition` with embedded reusable stage definitions, canonical future live-stage status contract (`not_started`, `in_progress`, `waiting`, `blocked`, `completed`, `skipped`), idempotent starter workflow seeding, deterministic resolver preview (explicit Order Item instance for future 11C, category workflow, tenant default, manual/no-workflow fallback), staff-only `/api/production-workflows` configuration endpoints, `production_workflow:read` / `production_workflow:manage`, audit events, and compact `/settings/production-workflows` UI. No live Work Order stages, stage timers, payroll integration, kiosk, Employee Portal production page, bottleneck analytics, advanced reports, or production-cost feedback were added. Targeted backend test file: `backend/tests/test_ec11_phase11a_production_workflows.py`; local pytest unavailable in both local Python runtimes, so GitHub Actions is the authoritative backend execution proof. Frontend production build passed. Full report: `/app/evidence/EC11_PHASE11A_COMPLETION_REPORT.md`.
- **EC11 Phase 11B (Production Timeline and Event History Foundation) — COMPLETE (2026-07-16); EC11 IN PROGRESS.** Added staff-only, read-only production timeline endpoints for Order, Order Item, and Work Order scope; a canonical projection service over existing source records; deterministic event identity/deduplication; filtering/pagination; visibility labels; customer-safe summaries; and compact Order/Work Order Activity tab UI. No new general audit system or timeline event collection was created. No live Work Order stages, stage timers, kiosk, Employee Portal production page, advanced analytics, pricing mutation, payroll mutation, payment mutation, invoice mutation, or production completion mutation was added. Targeted backend test file: `backend/tests/test_ec11_phase11b_production_timeline.py`; local pytest unavailable in both local Python runtimes, so GitHub Actions is the authoritative backend execution proof. Frontend production build passed locally. Full report: `/app/evidence/EC11_PHASE11B_COMPLETION_REPORT.md`.
- **EC11 Phase 11C (Work Order and Order Item Production Stage Integration) — COMPLETE (2026-07-16); EC11 IN PROGRESS.** Added live workflow instances and live production stage instances tied to Work Orders and production-required Order Items; item workflow override snapshots; idempotent stage generation; strict backend stage action endpoints; stage assignment through existing Employee/user eligibility checks; proof gate checks using existing proof/approval records; audit/timeline projection for workflow and stage events; notifications through existing notification service; and compact Work Order detail Stages tab. No production timers, payroll time entries, labor contribution records, kiosk, Employee Portal production actions, Production Board redesign, advanced analytics, pricing mutation, Quote mutation, Order financial mutation, or Order Item pricing mutation were added. Targeted backend test file: `backend/tests/test_ec11_phase11c_production_stages.py`; local pytest unavailable in both local Python runtimes, so GitHub Actions is the authoritative backend execution proof. Frontend production build passed locally. Full report: `/app/evidence/EC11_PHASE11C_COMPLETION_REPORT.md`. **Phase 11D NOT STARTED; phases 11G/11H NOT AUTHORIZED; EC12 NOT STARTED.**

### P1 — EC8 (all phases 8a–8f complete)
- Phase 8a — Employees & Team Foundation ✅ DONE
- Phase 8b — Time Clock & Timesheets ✅ DONE
- Phase 8c — Scheduling & Employee Portal ✅ DONE
- Phase 8d — Payroll (pay periods, transactions ledger, advances/payments/carryover, My Pay, exports) ✅ DONE
- Phase 8e — Equipment, Training & Certification + Work Order assignment enforcement ✅ DONE
- Phase 8f — Full EC8 frontend regression, full backend regression, closure evidence ✅ DONE

### Permanent scope carried into EC9/EC10 (owner-locked, unaffected in substance by the renumbering)
- **EC9 — Pricing Foundation Verification & Full Calculator Category Coverage** (formerly EC3.1) — every calculator category + formulas, shop rate/labor/materials/waste/markup/margin/minimums/complexity/add-ons/templates, snapshots, and pytest coverage. Controlling doc: `EC09_Pricing_Foundation_Calculators_and_Order_Pricing.docx`.
- **EC10 — Order Intake Capture & Visual Markup + Customer Decision Room + Templates** (formerly EC6.3, now expanded) — image/camera/PDF uploads, drawing/markup, versioned annotations, in-person signature capture, plus the new Customer Decision Room and reusable-template system. Controlling doc: `EC10_Order_Intake_Visual_Markup_Decision_Room_and_Templates.docx`.
- **EC7 A.3 Supplier Catalog, Price Comparison & Integrated Purchasing** — ✅ DELIVERED inside EC7 (unaffected by this intake).

### P1 — EC9–EC13 (per new pack)
- EC9 Phases 9A–9D COMPLETE (2026-07): Pricing Architecture/Global Foundation, Grouped Quiz, Materials & Saved Items UI.
- EC9 Phase 9E-1 COMPLETE (2026-07): Banners, Rigid Signs, Digital Print, Cut Vinyl.
- EC9 Phase 9E-2 COMPLETE (2026-07): Apparel, Promotional Items (incl. Business Cards), + 2 owner-required corrections (decoration-method pricing authority labeling, idempotent additive default merge).
- EC9 Phase 9E-3 COMPLETE (2026-07): Vehicle Graphics & Wraps.
- EC9 Phase 9E-4 COMPLETE (2026-07): Services (existing engine wired in + Labor Role override) and Custom/Miscellaneous (new strict manual fallback).
- EC9 Phase 9F — Quote/Order/Order Item pricing integration — COMPLETE (2026-07).
- EC9 Phase 9G — Snapshot and advisory contracts — COMPLETE (2026-07).
- EC9 Phase 9H — Review, testing, and closure — **COMPLETE (2026-07). EC9 fully CLOSED.**
- EC10 Order Intake, Visual Markup, Customer Decision Room, Templates.
- EC11 Production Timeline, Workflow Configuration, Stage Tracking, Kiosk.
- EC12 Tasks, Kanban, Messages, Notes, Calendar, Appointments, Community.
- EC13 Commercial Billing, Entitlements, Fees, Trials, Setup Packages (contradictions C1/C2 RESOLVED 2026-02 by owner decision — see hold register).

### P2 — Add-ons and AI (held on H2/H3/H4/H5/H8 — see `/app/memory/owner_specification_hold_register.md`)
- **EC14 Webstores** — requires separate owner authorization (H2).
- **EC15 Wrap Lab** — requires separate owner authorization (H3); standalone sale additionally requires its own preflight (H6).
- **EC16 Shared AI Gateway, Usage, Cost, Credits, Governance** — requires separate authorization (H4).
- **EC17 Studio AI Tools, Prompt Library, Generated Assets, AI Activity** — requires separate authorization (H4) AND is blocked until the owner completes the AI Tools Keep/Combine/Change/Rename/Defer/Remove review (H5/H8). Pricing-model question (C3) is RESOLVED — no separate AI subscription tiers (see hold register); this does not lift H5/H8.
- **EC18 Paid Business Assistant, Actions, Intelligence, Realtime Voice** — requires separate authorization (H4).
- **EC19 Onboarding, Help Center, Contextual Help, App Documentation.**
- **EC20 Platform Admin, Analytics, Dunning, Support.**
- **EC21 Marketing Website, Public Pricing, Founder Offer, Signup.**

### P3 — Final gate
- **EC22 Final Integration and Commercial Release Hardening** — closes only after EC9–EC21 are all COMPLETE.

### Naming corrections registered (2026-02, not yet applied to code)
- "Order Portal" → **Webstores** / "Webstores Manager". "Portal Owner" (Webstores context) → **Webstore Owner**.
- "Wrap Command Center" → **Wrap Lab**.
- See `/app/memory/documentation_authority_register.md` for details. No file/route/model/label has been renamed yet — renaming is deferred to the owning checkpoint (EC14, EC15).

### Commercial pricing source of truth (SUPERSEDED — see hold register; contradictions now resolved)

Old master plan **Appendix A.4** and `/app/docs/commercial/REVISED_COMMERCIAL_SOURCE_OF_TRUTH_2026-07.md` are now **superseded — historical reference only** by the new pack's `EC13_Commercial_Billing_Fees_Trials_Setup_and_Entitlements.docx` (plus `EC19` for setup-fee display and `EC21` for public-pricing rules). Almost all numeric values are confirmed unchanged. **Contradictions C1 (standalone pricing) and C2 (dunning model) are now RESOLVED by explicit owner decision (2026-02)** — see `/app/memory/owner_specification_hold_register.md` for the final figures (Webstores standalone $109/mo, Wrap Lab standalone $139/mo, both provisional with annual "not yet approved"; day-based delinquency model Days 1-7/8-14/15+ eligible-for-suspension, Stripe-webhook-driven, not retry-count-driven). **None of EC13 is implemented by this decision — documentation only.**

### Permanent future-scope register — configurable paid add-ons

- **Advanced Production Tracking & Bottleneck Analytics Add-On** (owner-locked, added 2026-07 during EC8 Phase 8c) — Production Stage Timer tracking Employee time against Work Order/Order Item/production stage/Equipment, distinct from and never merged with EC8's Payroll Time Clock. Append-only timer sessions/events, duplicate-timer/overlap/cross-tenant/unauthorized-WO prevention, no silent historical edits, no automatic production-time→payroll-time conversion without an explicit policy. Required analytics: estimated vs actual, average time by stage/category/employee, wait time, WIP age, queue length, stalled WOs, bottleneck stages, rework frequency, first-pass completion rate, equipment delays, throughput trends. Employee Portal integration reserves `/portal/employee/production` (not built, not wired into nav/routes). Gated through the shared plan/feature-entitlement system — **never** hardcoded as always-on. Full boundary contract documented at `/app/docs/production_stage_timer_boundary.md`. **NOT scheduled for implementation during EC8** — no code, routes, models, or placeholder data exist.

## Known deferred items (from EC3)

- Sales-tax provider integration (accepted as pass-through data).
- Assigned-team / install-notes / packaging-notes fields on OrderItem → owning module checkpoints (Team & Workflow, Wrap Lab).
- Public/portal quote approval → EC6 shared Approvals system.

## Authority references

**EC9 onward (current authority):**
- `/app/specs_pack/extracted/*.docx` — SignGuy AI Checkpoint Specification Pack (Master Index + EC09–EC22)
- `/app/memory/documentation_authority_register.md` — full priority order of every planning document
- `/app/memory/checkpoint_reference_table.md` — EC9–EC22 status + controlling doc + holds
- `/app/memory/owner_specification_hold_register.md` — active owner holds + open contradictions
- `/app/memory/permanent_backlog_register.md` — committed permanent-scope backlog

**EC0–EC8 (historical, COMPLETE/CLOSED — still authoritative for that range):**
- `/app/SIGNGUY_AI_FINAL_CONSOLIDATED_MASTER_BUILD_PLAN.md` (EC9–EC14 sections inside it are SUPERSEDED — see banners inline)
- `/app/SIGNGUY_AI_FINAL_SCOPE_AND_DECISION_REGISTER.md`
- `/app/SIGNGUY_AI_FEATURE_READINESS_MATRIX.md`
- `/app/SIGNGUY_AI_REPOSITORY_AND_ARCHITECTURE_SOURCE_MAP.md`
- `/app/memory/AGENT_INSTRUCTIONS.md` (permanent LOCKED technical rules — unaffected by the new pack)
- `/app/memory/progress_register.md`
